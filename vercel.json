{
    "rewrites": [
      {
        "source": "/:path((?!api/).*)",
        "destination": "/index.html"
      },
      {
        "source": "/:path(api/.*)",
        "destination": "/api"
      }
    ],
    "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }]
} 