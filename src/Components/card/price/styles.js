import styled from "styled-components";

export const Wrapper = styled.p`
  font-family: Inter, sans-serif;
  color: #fff;
  font-size: 1.6rem;
  font-weight: 600;
  span {
    color: #e1cb2b;
  }
`;
