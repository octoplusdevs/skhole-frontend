import { SpecialTitle } from "./components/special-title";

export default function NotFound() {
  return (
    <div className="w-full h-[100dvh] flex items-center justify-center">
      <SpecialTitle content="Pagina não encontrada" />
    </div>
  );
}
